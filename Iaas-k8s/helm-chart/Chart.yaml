apiVersion: v2
name: "{{ .Values.companyName }}-rafiki" # Modified to use companyName
description: A Helm chart for deploying {{ .Values.companyName }} Rafiki application suite # Modified to use companyName
type: application
version: 0.2.0
appVersion: "1.0.0" # Corresponds to the application version being deployed
kubeVersion: ">=1.25.0-0 <1.32.0-0" # Updated to support newer Kubernetes versions

# Maintainers (optional)
maintainers:
  - name: Regedit-msc
    email: your.email@example.com
# Dependencies (if any, e.g., a Redis chart)
# dependencies:
#   - name: redis
#     version: "17.x.x"
#     repository: "https://charts.bitnami.com/bitnami"
#     condition: redis.enabled # If deploying Redis as a sub-chart
